<view class="icon {{placement ? placement : ''}} u-class " catch:tap="onIconClick">
  <image src="{{imgSrc}}" style="width: {{width + 'rpx'}}; height:{{width+'rpx'}}" mode="widthFix"/>
  <view class="text color">{{name}}</view>
</view>
