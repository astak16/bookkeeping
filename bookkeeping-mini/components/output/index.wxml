<view class="output" style="background-color: {{tag.color}}">
  <u-icon class="icon" u-class="name"
          iconId="{{tag.id}}" name="{{tag.name}}" placement="right"
          width="86"
  ></u-icon>
  <view class="price">￥{{price}}</view>
</view>
